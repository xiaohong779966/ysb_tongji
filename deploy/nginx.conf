# 放在 /etc/nginx/conf.d/duty.conf
# 把 duty.example.gov.cn 换成你的域名；证书可用 certbot 申请

server {
    listen 80;
    server_name duty.example.gov.cn;
    # 强制跳转 HTTPS（证书办好后再启用）
    # return 301 https://$host$request_uri;

    client_max_body_size 1m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# server {
#     listen 443 ssl;
#     server_name duty.example.gov.cn;
#     ssl_certificate     /etc/letsencrypt/live/duty.example.gov.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/duty.example.gov.cn/privkey.pem;
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     }
# }
